services:
  db:
    # We use a mariadb image which supports both amd64 & arm64 architecture
    image: mariadb:10.6.4-focal
    # If you really want to use MySQL, uncomment the following line
    #image: mysql:8.0.27
    container_name: wordpress-demo-db
    command: '--default-authentication-plugin=mysql_native_password'
    volumes:
      - /automation/docker/wordpress/journal/db_data:/var/lib/mysql
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=uI0p5s4oJUA3
      - MYSQL_DATABASE=wordpress
      - MYSQL_USER=wordpress
      - MYSQL_PASSWORD=85zr5o6DZ6MI
    expose:
      - 3306
      - 33060
    networks: 
      - dockernet
  wordpress:
    image: wordpress:latest
    volumes:
      - /automation/docker/wordpress/journal/wp_data:/var/www/html
    ports:
      - 8089:8089
    container_name: wordpress-journal-main
    restart: always
    environment:
      - WORDPRESS_DB_HOST=wordpress-journal-db
      - WORDPRESS_DB_USER=wordpress
      - WORDPRESS_DB_PASSWORD=85zr5o6DZ6MI
      - WORDPRESS_DB_NAME=wordpress
    networks: 
      - dockernet
networks: 
  dockernet: 
    external: true
